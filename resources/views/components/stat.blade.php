@props(['label', 'value'])

<div class="bg-white rounded-xl border border-neutral-200 shadow-sm p-5">
    <div class="text-sm text-neutral-500">{{ $label }}</div>
    <div class="mt-2 text-2xl font-bold text-neutral-900">{{ $value }}</div>
</div>
